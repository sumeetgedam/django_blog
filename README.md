# django_project
This Django Blog application is an implementation of Corey Schafer Django YoutTube Playlist
To run this application:
  1. clone this repo into your local machine
  2. move into that directory(django_project) on command prompt
  3. and then type --> python manage.py runserver
  4. check http:127.0.0.1:8000/ on your browser
